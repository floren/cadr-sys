;;; -*- Mode: Lisp; Package: SYSTEM-INTERNALS; Base: 8.; Patch-File: T -*-
;;; Written 6/27/84 03:42:25 by RpK,
;;; Reason: This sets the device for OZ to OZ:, which will make everything win and
;;; the masses cheer with a fervor unknown in the history of pathname
;;; translation.  Note that for this to really work, site files with the new
;;; translations should have been loaded (this will happen by default).
;;; while running on Lisp Machine Eighteen from band 2
;;; with Don't-dump-a-band! Inconsistent (unreleased patches loaded) System 98.64, CADR 3.7, ZMail 53.17, MIT-Specific 22.1, microcode 309, ZM MIT.


;;; This sets the device for OZ to OZ:, which will make everything win and the masses
;;; cheer with a fervor unknown in the history of pathname translation.  Note that for this
;;; to really work, site files with the new translations should have been loaded (this
;;; will happen by default).

(SET-IN-INSTANCE (SI:PARSE-HOST "MIT-OZ") 'SI:PRIMARY-DEVICE "OZ")

;;; Uncache the translations

(SI:SET-SYS-HOST "OZ")
